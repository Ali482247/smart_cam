# Отчёт по приложению съёмки — сессия 2026-08-03 (сайнер Шоира, лист 1, слова 1–100)

Материал: `data/raw_v2/2026-08-03_camera_01 / _02 / _03`, 370 файлов.
Формат: `device_{номер_камеры}_{ГГГГММДД}_{ЧЧММСС}_{номер_записи}.mp4`, 1080×1920, ~29.92 fps.

---

## Что работает хорошо

**Номер записи общий для всех трёх камер и зашит в имя файла.** Это главное — сопоставление
«видео ↔ запись» больше не надо восстанавливать постфактум. Всего в сессии 126 записей,
номера 10–135, и все три камеры согласны, какой номер какому моменту соответствует.

Таймкод в имени тоже общий и точный — расхождение между камерами не обнаружено.

---

## БАГ 1 — камера теряет команду «стоп/старт», две записи склеиваются в один файл

Камера пропускает конец текущей записи и начало следующей. В результате:
- файл текущей записи содержит **две записи подряд** (и паузу между ними);
- файла следующей записи на этой камере **вообще нет**, её номер пропущен.

### Случай A — запись 16

| камера | файл | длительность | что внутри |
|---|---|---|---|
| 01 | `device_1_20260803_063244_15.mp4` | 5.65 s | только запись 15 ✅ |
| 01 | `device_1_20260803_063308_16.mp4` | 9.46 s | только запись 16 ✅ |
| 02 | `device_2_20260803_063244_15.mp4` | **33.59 s** | записи 15 **и** 16 ❌ |
| 02 | — | — | файла `_16` нет ❌ |
| 03 | `device_3_20260803_063244_15.mp4` | **33.39 s** | записи 15 **и** 16 ❌ |
| 03 | — | — | файла `_16` нет ❌ |

### Случай B — запись 85

| камера | файл | длительность | что внутри |
|---|---|---|---|
| 01 | `device_1_20260803_071029_84.mp4` | 5.95 s | только запись 84 ✅ |
| 01 | `device_1_20260803_071046_85.mp4` | 10.66 s | только запись 85 ✅ |
| 02 | `device_2_20260803_071029_84.mp4` | **27.57 s** | записи 84 **и** 85 ❌ |
| 02 | — | — | файла `_85` нет ❌ |
| 03 | `device_3_20260803_071029_84.mp4` | 5.28 s | только запись 84 ✅ |
| 03 | `device_3_20260803_071046_85.mp4` | 10.49 s | только запись 85 ✅ |

### Доказательство, что это именно склейка, а не что-то другое

Файл `_15` на камере 02 начинается в 06:32:44 и длится 33.59 s → заканчивается в **06:33:17.6**.
На камере 01 запись 16 начинается в 06:33:08 и длится 9.46 s → заканчивается в **06:33:17.5**.
Сходится с точностью до кадра. То же самое для случая B.

Дополнительно: если найти внутри склеенного файла момент жеста, он оказывается ровно там,
где предсказывает разница таймкодов (24.0 s для случая A, 17.0 s для случая B), и на всех трёх
камерах в этот момент один и тот же кадр одного и того же жеста.

---

## БАГ 2 — камера теряет запись целиком, файла нет вообще

Здесь склейки НЕТ: соседний файл нормальной длины (≈4.5 s), номер просто пропущен.
То есть камера не начала запись вообще.

| запись | нет на камерах | осталось камер | соседний файл нормальной длины? |
|---|---|---|---|
| 96  | 01 | 2 | да (`_95` на кам.01 = 4.58 s) |
| **97** | **01 и 02** | **1 — только камера 03** | да (`_96` на кам.02 = 4.65 s) |
| 101 | 03 | 2 | да (`_100` на кам.03 = 5.85 s) |
| 125 | 01 | 2 | да (`_124` на кам.01 = 4.48 s) |

**Запись 97 снята одной камерой из трёх.** Для проекта это критично: распознавание
работает голосованием по трём ракурсам, с одной камерой такой материал сильно слабее.

---

## БАГ 3 (вероятный) — «стоп» приходит раньше, чем закончен жест

Запись 85: жест начинается на 7.1 s и продолжается **до последнего кадра файла** (10.7 s)
на всех камерах одновременно. Похоже, что запись оборвана на середине жеста.
Нужно проверить глазами; если подтвердится — стоп срабатывает без запаса.

---

## Возможная зацепка — короткая пауза между записями

Медианная пауза между записями в сессии — 14.4 s. Но:

| запись | пауза перед ней | что случилось |
|---|---|---|
| 96 | **5.4 s** (короче, чем у 92 % записей) | камера 01 не сняла |
| 97 | **5.3 s** (короче, чем у 94 % записей) | камеры 01 и 02 не сняли |

Две из четырёх потерь — подряд, в самом плотном месте съёмки. Похоже, камера не успевает
поднять новую запись, если команда приходит слишком быстро после предыдущей.

Но это объясняет не всё: запись 101 потеряна на камере 03 после паузы в **144 s**, а
запись 125 — после 8.8 s. И наоборот, перед записями 106 (3.2 s), 129 (3.8 s), 117 (3.8 s)
паузы были ещё короче, и там всё снялось. Так что короткая пауза — фактор риска,
а не единственная причина. Нужны логи со стороны приложения.

## Сводка по камерам

| камера | всего файлов | проблемы |
|---|---|---|
| 01 (`device_1`, левая) | 123 | потеряны записи **96, 97, 125** |
| 02 (`device_2`, центр) | 123 | склейки **15+16**, **84+85**; потеряна запись **97** |
| 03 (`device_3`, правая) | 124 | склейка **15+16**; потеряна запись **101** |

Итого из 126 записей × 3 камеры = 378 ожидаемых видео получено **370**, из них 3 лежат
внутри чужих файлов. Полноценных, готовых к работе: **367 из 378 (97.1 %)**.

---

## Что просим сделать

1. **Подтверждение записи (ack) на каждую команду.** Камера должна отвечать
   «начал запись N» / «закончил запись N». Если ответа нет — повтор команды.

2. **Сайдкар-JSON рядом с каждым файлом** в момент записи: номер записи, реальные
   время старта и стопа, номер камеры. Тогда склейку видно сразу, а не постфактум.

3. **Отчёт в конце сессии:** «ожидалось N записей; камера 01 — N файлов, камера 02 — N,
   камера 03 — N; расхождения: …». Оператор должен узнать о потере на площадке,
   а не через неделю при разборе.

4. **Запас на стоп** — дописывать 0.5–1 s после команды «стоп», чтобы не срезать конец жеста.

5. **Вопрос:** нумерация записей в этой сессии началась с 10, а не с 1. Счётчик сквозной
   по устройству или сбрасывается на сессию? Нужно знать, чтобы следующие съёмки
   не перепутались между собой.

---

# Дополнение 2026-08-12 — версия приложения 1.1.0, три съёмочных дня

Считано по журналам `logs_from_app/recording_log_20260810 / 11 / 12.ndjson`.

## Сводка отказов

| день | записей | файлов ждём | 0 байт | не пришло подтверждение | доля дефектных файлов |
|---|---|---|---|---|---|
| 10.08 | 632 | 1896 | 6 | 3 | 0.47 % |
| 11.08 | 1208 | 3624 | 9 | 4 | 0.36 % |
| 12.08 | 683 | 2049 | 11 | 1 | 0.68 % |
| **всего** | **2523** | **7569** | **26** | **8** | **0.48 %** |

Запись считается потерянной, если провалились все три камеры:

| день | 3 камеры | 2 | 1 | 0 | потеряно |
|---|---|---|---|---|---|
| 10.08 | 629 | 0 | 0 | 3 | 0.47 % |
| 11.08 | 1203 | 1 | 0 | 4 | 0.33 % |
| 12.08 | 678 | 1 | 1 | 3 | 0.44 % |

**Все десять потерянных записей оператор переснял сразу** — ошибка видна на экране.
Безвозвратных потерь за три дня нет. Цена отказов — пересъёмы и потеря доверия
к системе, а не утрата материала.

## ✅ Закрыто: нулевой файл больше не помечается `ok`

Прежний отчёт фиксировал, что приложение отдаёт `status: ok` на файл нулевого размера.
Проверено по всем трём дням: **26 нулевых файлов, помечены `ok` — ноль**, у всех
`status: error`. Баг закрыт, спасибо.

## ❌ Вернулся: сдвиг подтверждения на одну запись (объявлен исправленным в 1.1.0)

12.08, камера 1, конец смены. Подтверждения `STOP_SAVE` поехали на одну запись:

```
16:33:58.224  STOP  зап.2501
16:33:58.254  STOP_SAVE device_1 → файл ..._2501.mp4        ✅ вовремя
16:34:41.067  STOP  зап.2502
16:34:41.103  STOP_SAVE device_1 → файл ..._2501.mp4 ПОВТОР ← сдвиг начался
16:34:53.658  STOP  зап.2503
16:34:53.725  STOP_SAVE device_1 → файл ..._2502.mp4        ← закрыт на 12.7 с позже своего стопа
              файла зап.2503 на камере 1 нет вовсе
```

Физическое следствие — склейка: файл записи 2502 длится **11.4 с вместо 0.8 с** и
содержит внутри себя обе записи, 2502 и 2503. Дальше камера выправилась сама.

**Почему это хуже нулевых файлов.** Нулевой файл виден сразу: оператор переснимает,
потерь нет. Склейка проходит молча — файл есть, имя правильное, размер нормальный.
Обнаруживается она только отдельным инструментом, который сверяет длительность файла
с журналом. Ровно такая тихая порча в прошлом году стоила нам двух волн съёмок.

**Признак для ваших логов:** `STOP_SAVE` пришёл позже своего `STOP` более чем на
несколько секунд. По этому признаку за три дня нашлись обе аномалии 12.08 и ни одной
ложной на 10.08 и 11.08.

**Просьба:** если камера не успела закрыть файл к своему `STOP`, отдавать `status: error`,
как это уже сделано для нулевых файлов. Тогда оператор переснимет на месте, и склейка
не доедет до датасета.

---

# Дополнение 2026-08-15 — ❌ номер устройства не закреплён за телефоном

## Что произошло

Съёмка 14.08, **123 записи из 1135**: два телефона писали файлы под номерами друг друга.

| где лежит файл | как назван | сколько записей |
|---|---|---|
| папка `camera_01` (телефон 1) | `..._device_2_...` | 123 |
| папка `camera_02` (телефон 2) | `..._device_1_...` | 118 |
| папка `camera_03` | `..._device_3_...` | не затронута |

Обмен парный и полный: в 114 случаях у одной записи в первой папке лежит `device_2`,
во второй `device_1`, в третьей `device_3`. Ещё 5 записей — второй телефон не снял
вовсе, 4 записи — не снял третий. Отсюда и разница 123 против 118: 123 − 5 = 118.

## Почему это точно приложение, а не перенос

Материал переносится потелефонно: аппарат подключают, заводят папку `camera_0N` и
копируют его содержимое целиком. При таком порядке в папку физически не могут попасть
файлы другого телефона — а тем более выборочно, 123 штуки из полутора тысяч, да ещё
так, чтобы у каждой записи обмен оказался парным.

Значит номер в имени присвоен неверно **в момент записи**, самим приложением.

**Двенадцать предыдущих съёмок (03.08–13.08, около 13 000 файлов) чисты полностью** —
ни одного расхождения. То есть это не постоянное поведение, а срыв на одном отрезке.

## Чем это опасно

Ракурс камеры — часть данных, а не оформление: модель на инференсе усредняет
предсказания трёх камер (−30°, 0°, +30°), и это даёт +9 процентных пунктов точности.
Клип с неверно подписанным ракурсом отравляет и обучение, и голосование.

Сейчас обошлось: телефоны стояли на штативах и не двигались, а папка соответствует
физическому аппарату — поэтому раскладку мы ведём по папке, и ракурсы не пострадали.

**Но повезло только потому, что обмен был парным и полным.** Если бы съехал один номер
из трёх, файл лёг бы в свою папку под чужим именем, расхождение оказалось бы
несимметричным, и мы бы его не заметили. Ровно такая незаметная порча в прошлом году
стоила проекту двух волн съёмок.

## Просьба

1. **Привязать идентификатор к устройству намертво** — к серийному номеру или другому
   стабильному признаку, а не к порядку подключения или очерёдности регистрации.
2. **Писать этот признак в журнал при старте сессии** — чтобы расхождение было видно
   сразу, а не всплывало через сутки при разборе.
3. Если номер по какой-то причине переназначается на ходу — сообщать об этом событием
   в журнале, а не молча.

---

# Дополнение 2026-08-17 — номера устройств разъезжаются уже внутри слова

## Стало хуже, а не лучше

14.08 два телефона обменялись номерами разом, на записи 5102, и обмен был парным —
поэтому мы его и заметили. **16.08 номера разъезжаются внутри одного слова.**

Слово `0601_Ketnioq`, три папки (по одной на телефон):

| папка | дубль a | дубль b |
|---|---|---|
| 1 | — | `device_1` |
| 2 | `device_3` | `device_2` |
| 3 | `device_1` | `device_3` |

На дубле `a` второй телефон назвался третьим, а третий — первым. На дубле `b`, снятом
через несколько секунд, всё встало по местам. То есть идентификатор переназначается
между соседними записями одного слова.

## Почему это красная линия

Ракурс камеры — не оформление, а часть данных. На инференсе предсказания трёх камер
(−30°, 0°, +30°) усредняются, и это даёт **+9 процентных пунктов** точности. Клип
с неверно подписанным ракурсом отравляет и обучение, и голосование.

Пока нас спасает то, что **папка соответствует физическому телефону**: аппарат
подключают и копируют его содержимое целиком. Мы поэтому и ведём раскладку по папке,
а не по имени файла. Но это костыль: он держится на процедуре переноса, а не на данных.

⚠ И он не защищает от второй беды. 14.08 телефоны вдобавок **стояли не на своих точках**:
папка `camera_02` весь день отдавала левый ракурс вместо фронтального. Ни имя файла,
ни папка этого не показывают — обе подписи врут согласованно. Нашли случайно, глазами,
когда заказчик увидел, что человек на деке снят вполоборота.

## Просьба, по убыванию важности

1. **Привязать идентификатор к аппарату намертво** — к серийному номеру или другому
   стабильному признаку. Не к порядку подключения и не к очерёдности регистрации.
2. **Писать этот признак в журнал при старте каждой сессии.** Тогда расхождение видно
   сразу, а не через двое суток при разборе.
3. **Показывать оператору на экране, какой телефон какой точке соответствует** — чтобы
   перестановка аппаратов ловилась до съёмки, а не после.

## Остальное из этой же партии

- **Ложное «сохранено»** — журнал сообщает об успешной записи, а файла на диске нет.
  14 клипов у Шахзоды за 14.08. Опаснее ложной тревоги: та заставляет переснять зря,
  а это молча оставляет дырку в датасете.
- **Журналы** складывать в отдельную папку и создавать сразу после сессии, а не отдавать
  по запросу на следующий день. Сейчас разбор съёмки задерживается на сутки.
- **Возможность завести нового сайнера.** Азиза 13.08 снималась под номером Нигоры,
  потому что добавить человека было нельзя. Её записи пришлось переназначать вручную.
  Заодно поправить имя: сейчас приложение пишет `signer_11_Нигора_под_вопросом`.
- **Пропал прицел** (рамка кадрирования) и кадр разворачивается горизонтально при старте.
