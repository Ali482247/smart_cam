# server/protocol

Generated/compiled Python protobuf bindings plus Envelope encode/decode helpers used by
`server/network`. Distinct from the top-level `protocol/` directory, which holds the
canonical `.proto` schema *source* shared conceptually with the Dart client.

No schemas exist yet — see `docs/protocol_specification.md` for the full Envelope design.
